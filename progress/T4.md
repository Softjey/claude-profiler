## T4 — Event model — PASS
Verified: 2026-09-17

| Acceptance criterion | Evidence | Result |
|---|---|---|
| Two `tool_use` blocks in one assistant message produce two events sharing a start time | `pnpm vitest run src/model` → `buildEventModel > emits two tool_use events sharing a start time for parallel calls` passed; asserts `toolUses[0].startedAt === toolUses[1].startedAt` and distinct `durationMs` (1000 / 2000) from separate `tool_result` timestamps | PASS |
| An interrupted transcript (`tool_use` with no result) yields `durationMs: null`, not a crash | `buildEventModel > leaves an unmatched tool_use with durationMs null and unfinished true, without crashing` passed; asserts `durationMs === null`, `unfinished === true`, no thrown error | PASS |
| `turnIndex` is monotonic and starts at 0 | `buildEventModel > assigns a monotonic turnIndex starting at 0` passed; asserts `turnIndexes === [0, 0, 1, 1]` and non-decreasing sequence, first value `0` | PASS |

Baseline: `pnpm verify` → build + full suite green (2 test files, 10 tests).
Task verify (`pnpm vitest run src/model`): 4/4 tests passed.

Convention check:
- Layer direction: `src/model/*` imports only from `../parse/types.js` (lower layer per `cli → tui → artifact → metrics → model → parse`), nothing higher imported. OK.
- Naming: `durationMs` ends in `Ms`; timestamp fields (`at`, `startedAt`) end in `At`. Initially used a bare `timestamp` field on `AssistantEvent`/`UserPromptEvent`/`SystemEvent` — caught during this verification pass and renamed to `at` before recording PASS.
- Never throws: `buildEventModel` has no throw paths; unmatched/untimed records are skipped or left partially filled, not errored.
- Honesty rule: no comment or label calls `durationMs` exact; doc comment says "matching... and computing durationMs", not claiming precision.

Notes: `ToolUseEvent`, `AssistantEvent`, `UserPromptEvent`, `SystemEvent` all use `undefined`-inclusive field types (not optional `?:` keys) to satisfy `exactOptionalPropertyTypes: true` in `tsconfig.json`. `ContentBlock` union narrowing required explicit type guards (`isToolResultBlock`, `isToolUseBlock`) since `UnknownContentBlock.type` is a plain `string`, not a literal, so `block.type === "tool_use"` alone didn't narrow.
