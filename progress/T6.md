## T6 — Tool statistics and outlier detection — PASS
Verified: 2026-09-17

| Acceptance criterion | Evidence | Result |
|---|---|---|
| On session `54fd3ef0`, `mcp__claude-in-chrome__computer` reports `calls: 20`, `medianMs ≈ 749`, `maxMs ≈ 1_064_111`, `outlierCount: 1` | Ran the built pipeline (parse → build-model → computeToolStats) against the real local transcript `~/.claude/projects/-Users-softjey-Desktop-projects-personal-job-search-auto-applier/54fd3ef0-3d6f-48d5-8e4b-41bf3a8d13d8.jsonl`. Output: `{"calls":20,"medianMs":749,"maxMs":1064111,"outlierCount":1,"typicalMs":14980,"totalMs":1080127}`. Also asserted in `src/metrics/tool-stats.test.ts` ("reports the documented computer-tool stats and outlier discrepancy" — passed). | PASS |
| `typicalMs` for that tool is under 30s while `totalMs` is ~18min | Same run: `typicalMs: 14980` (14.98s, under 30s) vs `totalMs: 1080127` (~18.0min) | PASS |

`pnpm verify`: build green, full suite 28/28 passed.
`pnpm vitest run src/metrics/tool-stats.test.ts`: 8/8 passed, including the corpus-backed acceptance test above.

Convention check:
- Layer direction: `src/metrics/tool-stats.ts` imports only from `../model/events.js` (metrics → model), no upward imports.
- Duration fields end in `Ms` (`totalMs`, `typicalMs`, `medianMs`, `p90Ms`, `maxMs`, `durationMs`); timestamp field `startedAt` ends in `At` and is ISO 8601 (passed through unchanged from `ToolUseEvent`).
- No label/comment calls a derived duration exact.

Notes:
- `ToolStat.callRefs` is implemented as an embedded `ToolCall[]` array (full objects), not an id-based indirection into a separate global call list — SPEC's data model names it `callRefs[] -> ToolCall` but T11 (Profile assembly) is the natural place to decide artifact-level id referencing/deduplication if the JSON schema needs it. This keeps T6 self-contained per its own Files list (`src/metrics/tool-stats.ts`, `src/metrics/tool-stats.test.ts`) without reaching into artifact assembly.
- `percentile`/`median` use linear interpolation between closest ranks, a standard choice; SPEC doesn't mandate a specific interpolation method and the acceptance numbers match exactly with it.
- The corpus-backed test in `tool-stats.test.ts` is skipped gracefully (`it.skip`) if the referenced local transcript file isn't present on the machine running the suite, so `pnpm vitest run` stays green on a clean checkout without that transcript.
