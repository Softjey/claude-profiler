## T3 — Corpus smoke test — PASS
Verified: 2026-09-17

| Acceptance criterion | Evidence | Result |
|---|---|---|
| All local transcripts parse with zero thrown errors. | `pnpm vitest run test/corpus.test.ts --reporter=verbose` → "files: 726", test passes, no thrown error anywhere in the run. | PASS |
| The summary reports at least the versions 2.1.219–2.1.273. | Same run's summary line: `versions: 2.1.219, 2.1.220, 2.1.223, 2.1.224, 2.1.233, 2.1.234, 2.1.235, 2.1.237, 2.1.239, 2.1.240, 2.1.241, 2.1.250, 2.1.251, 2.1.252, 2.1.259, 2.1.260, 2.1.263, 2.1.266, 2.1.267, 2.1.268, 2.1.269, 2.1.270, 2.1.271, 2.1.272, 2.1.273` — both `2.1.219` and `2.1.273` present; test asserts both explicitly. | PASS |
| Baseline: `pnpm verify` (build + full unit suite) | `tsc -p tsconfig.json` clean, `vitest run` → "Test Files 2 passed (2)", "Tests 7 passed (7)". | PASS |
| Skip-clean-machine behavior (step 1 of the task) | Ran with `HOME=/tmp/fake-home-empty` (no `.claude/projects`): suite logs "no .../\.claude/projects, skipping" and exits green — "Tests 1 passed \| 1 skipped (2)". | PASS |

Notes:
- Used `fs/promises.readdir(dir, { recursive: true, withFileTypes: true })` (Node ≥20.1) instead of
  a glob dependency — keeps this task's file list to just `test/corpus.test.ts`, no new dependency.
- The acceptance criterion is checked as the two boundary versions (`2.1.219`, `2.1.273`) actually
  observed in the local corpus, rather than every intermediate version, so the test doesn't become
  brittle if the local `~/.claude/projects` corpus is pruned or grows between runs.
- Left the extension point for T5's time-split invariant as a comment inside the per-file loop, so
  that edit is an append, not a rewrite, per the task's step 4.
- Local corpus stats at verification time: 726 files, 134,155 records, 67 skipped lines, 25 distinct
  versions, 13 distinct record types (including a `1.0.0` transcript, correctly parsed and outside
  the required range so not asserted on).
