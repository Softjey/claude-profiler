## T9 — Verify what hooks actually measure — PASS
Verified: 2026-09-17

| Acceptance criterion | Evidence | Result |
|---|---|---|
| `docs/hook-timing-findings.md` states plainly whether approval wait is isolatable, with the measured numbers. | File's "## Verdict" section: "**Approval wait is isolatable.**" "## Measured numbers" gives real timestamps for a captured `PreToolUse`/`PostToolUse` pair (`tool_use_id: toolu_0137SejqL6WBhDxkfjT9oBgV`): hook span 38.949s (`12:44:18.088Z` → `12:44:57.037Z`), transcript `tool_use → tool_result` span 39.013s, `PostToolUse.duration_ms` = 23ms, derived `approvalMs` ≈ 38926ms. | PASS |
| If not isolatable, the doc says so and T10's goal narrows (`approvalMs` dropped). | N/A — the finding is that approval wait **is** isolatable, so this branch doesn't apply. Doc's "## Verdict" explicitly says T10 does not need to narrow scope. | PASS (N/A branch) |

**Verify command:** manual — read `docs/hook-timing-findings.md` and confirm the measured ~30s+ delay appears in the hook span. Observed: the deliberate operator wait (~39s) appears almost exactly in the `PreToolUse → PostToolUse` hook span (38.949s vs transcript's 39.013s, ~64ms apart — spawn-overhead noise, not a structural gap).

**Baseline:** `pnpm verify` — 11 test files, 44 tests, all passed; build (`tsc`) clean.

Notes:
- Methodology deviated slightly from the literal steps: instead of a `Bash` tool needing approval, the captured approval-gated call was a `Skill` invocation (`update-config`) in a fresh session started with the hooks already registered. Still satisfies "a tool that requires approval" — `Bash` calls attempted mid-session in the *current* session's worktree did not trigger hooks at all (see next point), so a fresh session was required regardless of which tool triggered the prompt.
- Real finding beyond the spike's literal ask: hook config loads once at session launch, from the launch directory — a mid-session `cd` (e.g. via a worktree switch) does not pick up hooks registered afterward. Documented in the findings doc as a note for T10's `install-hooks`.
- Temporary hook scaffolding (`.claude/settings.local.json`, scratch probe script) was removed before this commit; only `docs/hook-timing-findings.md` is part of this task's diff, matching the Files list.
