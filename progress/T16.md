## T16 — README and packaging — PASS
Verified: 2026-09-17

| Acceptance criterion | Evidence | Result |
|---|---|---|
| `pnpm pack --dry-run` lists no source, no tests, no fixtures | `pnpm pack` tarball contents (`tar -tzf claude-profiler-0.1.0.tgz`): only `package/dist/**/*.js{,.map}`, `package/LICENSE`, `package/package.json`, `package/README.md` — no `.ts`/`.tsx`, no `*.test.*`, no fixture files | PASS |
| A reader who only reads the README cannot come away thinking tool durations are exact | README.md "What these numbers mean" section states plainly: "Tool durations include approval and idle wait", labels them "tool + approvals" never "tool time", and explains every sum is shown next to its median and outlier count | PASS |

Verify command: `pnpm pack && npx ./claude-profiler-0.1.0.tgz --help`
- `pnpm pack` succeeded, tarball as above.
- `npx ./claude-profiler-0.1.0.tgz --help` printed the usage text and exited 0.

Baseline: `pnpm verify` — build clean, 137/137 tests passed (31 test files).

Notes:
- `package.json`'s `files` field (`dist`, `README.md`) and `bin` entry were already correct
  from an earlier task; T16 added README.md, LICENSE, and `.github/workflows/ci.yml` without
  needing to touch `package.json`.
- CI workflow (`.github/workflows/ci.yml`) runs `pnpm install --frozen-lockfile` then
  `pnpm verify` on Node 20; the corpus test self-skips without `~/.claude/projects`, so no
  special-casing was needed in the workflow.
- The README's "Example Overview render" is an ASCII mockup of the actual TUI layout, not a
  captured screenshot — no real transcript fixture was available in this environment to
  launch the TUI against and capture genuine output. Labelled explicitly as an example, not
  a screenshot, to stay honest about what it is.
