## T13 — TUI: Overview tab — PASS
Verified: 2026-09-17

`pnpm verify`: build green, 115/115 unit tests passing (30 new in `src/tui`).

| Acceptance criterion | Evidence | Result |
|---|---|---|
| The four split values shown as percentages sum to 100% | Real run on session `54fd3ef0`: `37.6% + 32.2% + 27.3% + 2.9% = 100.0%`. Also unit-asserted in `TimeSplitBar.test.tsx` ("sums to 1" over the raw fractions, so rounding of the displayed percentages can't hide drift). | PASS |
| On session `54fd3ef0`, the `computer` row is visibly marked as outlier-dominated | Drove `node dist/cli/bin.js 54fd3ef0-3d6f-48d5-8e4b-41bf3a8d13d8` over a real pty (Python `pty.fork`, since the sandbox's own stdin isn't a TTY). Rendered row: `! mcp__claude-in-chrome__computer  18m00s  9.5%  20  749ms  1`. `totalMs` (1,080,000ms) is ~72x `typicalMs` (median 749ms × 20 ≈ 15,000ms), past the 3x threshold, and the `!` marker (bold red) renders on that row and no other visible row at that sort order. Also unit-tested in `ToolTable.test.tsx` with the same numbers. | PASS |
| No label anywhere calls a derived duration exact | Grepped rendered output and source: the only occurrence of "exact" is `"derived, not exact"` in the `TimeSplitBar` caveat, shown only while `precision === "derived"`. `Overview.test.tsx` asserts the word never appears outside that phrase. | PASS |
| `shell.ts` exposes tab registration and the nav stack, so T14/T15 need no `App.tsx` edit | `src/tui/shell.ts` exports `registerTab`/`getTabs` (module-level tab registry, side-effect registration on import — see `Overview.tsx`'s trailing `registerTab(...)` call) and `useNavStack`/`NavScreen`/`NavStack` (push/pop with per-frame selection). `App.tsx` only imports `./Overview.js` for its registration side effect and never references Overview's internals; `T14`/`T15` can add new tab files imported the same way, or call `nav.push(...)` from within a tab to add drill-down screens, without touching `App.tsx`. | PASS |

### Verify line
`node dist/cli/bin.js 54fd3ef0-3d6f-48d5-8e4b-41bf3a8d13d8` and navigate — ran over a real pty:
launch → header/split/table render → `↓` moves the row highlight → `s` cycles the sort
(`sorted by total` → `sorted by calls`, row order changes accordingly) → `q` exits cleanly
(no further frames, cursor restored). `/` filter and `⏎`/`⇥` are unit-tested
(`Overview.test.tsx`, `shell.test.tsx`) since `⏎`'s target (`ToolDetail`, T14) and `⇥`'s second
tab (T15) don't exist yet.

### Convention check
- Layer direction: `tui` imports only from `artifact` (the `Profile` type) and `hooks/sidecar.js`
  (`ExactToolStat`/`MergedTimeSplit`, the same types `artifact/profile.ts` already merges in) —
  no import of anything from `cli`. `cli/index.ts` now imports `tui/App.js`, consistent with
  `cli → tui`.
- Naming: `kebab-case.ts`/`.tsx` files, `PascalCase` components/types, `camelCase` functions;
  every duration field already ends in `Ms` (reused from existing `ToolStat`/`TimeSplit` types).
- Honesty rule: see the "exact" criterion above.
- Tests: Vitest + `ink-testing-library`, matching `SessionPicker.test.tsx`'s existing pattern.

Notes:
- `cli/index.ts` previously printed "the TUI is not implemented yet" and exited; T13 wires it up
  to actually render `App` when stdout is a TTY, and falls back to the old message (rephrased)
  otherwise — this was necessary for the Verify line to do anything, and is a strict widening of
  behavior only reachable once `App.tsx` exists, not a change to another task's file ownership.
- `⏎` (drill in) and the second/third tab are deliberately inert in T13: `Overview.tsx` doesn't
  push a screen on Enter (there's nothing to push to yet), and `App.tsx`'s tab bar only renders
  when `getTabs().length > 1`. T14 wires Enter by having `ToolTable`'s selected row build a
  `NavScreen` and calling `nav.push(...)`; T15 registers two more tabs the same way `Overview.tsx`
  registers itself.
