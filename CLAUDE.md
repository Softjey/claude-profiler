# Workflow

- Use **pnpm**, not npm: `pnpm install`, `pnpm verify` (build + unit tests), `pnpm vitest run <path>`. Never create a `package-lock.json`.
- Run `pnpm verify` before every commit.
- Write commit messages in English following Conventional Commits format.
